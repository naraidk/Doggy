<nav class="bottom-nav border-top bg-white d-flex justify-content-around align-items-center fixed-bottom py-3">

  <% if user_signed_in? %>
    <%= link_to root_path, class: "nav-icon fs-4 #{current_page?(root_path) ? 'text-doggy' : 'text-dark'}", title: "Accueil" do %>
      <i class="fa-solid fa-house"></i>
    <% end %>
  <% else %>
    <%= link_to new_user_session_path, class: "nav-icon fs-4 #{current_page?(new_user_session_path) ? 'text-doggy' : 'text-dark'}", title: "Accueil" do %>
      <i class="fa-solid fa-house"></i>
    <% end %>
  <% end %>

  <%= link_to "#", class: "nav-icon fs-4 #{current_page?('#') ? 'text-doggy' : 'text-dark'}", title: "Map" do %>
    <i class="fa-solid fa-map-location-dot"></i>
  <% end %>

  <%= link_to "#", class: "nav-icon fs-4 #{current_page?('#') ? 'text-doggy' : 'text-dark'}", title: "Calendar" do %>
    <i class="fa-regular fa-calendar-days"></i>
  <% end %>

  <%= link_to conversations_path, class: "nav-icon fs-4 #{current_page?(conversations_path) ? 'text-doggy' : 'text-dark'}", title: "Chatbot" do %>
    <i class="fa-solid fa-comment-dots"></i>
  <% end %>

  <% if user_signed_in? %>
    <%= link_to dogs_path, class: "nav-icon fs-4 #{current_page?(dogs_path) ? 'text-doggy' : 'text-dark'}", title: "Profil" do %>
      <i class="fa-solid fa-user"></i>
    <% end %>
  <% else %>
    <%= link_to new_user_session_path, class: "nav-icon fs-4 #{current_page?(new_user_session_path) ? 'text-doggy' : 'text-dark'}", title: "Profil" do %>
      <i class="fa-solid fa-user"></i>
    <% end %>
  <% end %>

</nav>
