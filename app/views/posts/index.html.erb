<% @feed.each do |item| %>
  <% if item.is_a?(Post) %>
    <%= render "posts/post", post: item %>
  <% else %>
    <%= render "events/feed_card", event: item %>
  <% end %>
<% end %>
<turbo-frame id="comments_panel"></turbo-frame>
<turbo-frame id="post_panel"></turbo-frame>
